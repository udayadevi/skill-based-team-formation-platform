# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# In production on Render, set this to your MongoDB connection string:
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.v9wrfvj.mongodb.net/skillbasedteam?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=skillbasedteam

# JWT Authentication Secret
JWT_SECRET=your_jwt_secret_key_here

# Frontend URL for CORS (comma-separated for multiple origins)
# Example: http://localhost:5173,https://your-frontend.vercel.app
FRONTEND_URL=http://localhost:5173

# Email Service (Optional: for OTP password reset notifications)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password
